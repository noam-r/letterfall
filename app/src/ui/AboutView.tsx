export function AboutView() {
  return (
    <div className="about-view">
      <h2>About LetterFall</h2>
      <p>LetterFall blends topic-driven word challenges with an arcade flow.</p>
      <p>This build is an early milestone focused on the project scaffold and UI shell.</p>
      <p>No data is collected; everything runs locally.</p>
    </div>
  );
}
